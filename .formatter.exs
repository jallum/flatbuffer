# Used by "mix format"
[
  inputs: [
    "{mix,.formatter}.exs",
    "{config,lib,test}/**/*.{ex,exs}",
    "integration/*/{mix,.formatter}.exs",
    "integration/*/{config,lib,test}/**/*.{ex,exs}"
  ]
]
